[pytest]
pythonpath = .
markers =
    unit: fast, isolated unit tests with no I/O
    integration: tests touching multiple components (files, API, mocked network)
    e2e: tests against a fully running instance of the app
